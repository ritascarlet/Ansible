all:
  children:
    iperf_servers:
      hosts:
        localhost:
          ansible_connection: local

    iperf_clients:
      hosts:
        server-1:
          ansible_host: 11.11.11.11
          ansible_user: root
          ansible_port: 22
          server_name: server-1
          status: prepared
          ansible_password: "{{ vault_ansible_password | default('CHANGE_ME') }}"
          os_name: Debian
          host_provider: Netcup
          location: Germany
          group: 3X-UI
